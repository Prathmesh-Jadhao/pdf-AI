# 📄 Doc AI

**DocuMind AI** is a Retrieval-Augmented Generation (RAG) application that enables users to interact with PDF documents using natural language. Instead of manually searching through lengthy documents, users can upload one or more PDFs, ask questions, and receive context-aware answers generated by a Large Language Model (LLM).

---

## 🚀 Features

* Upload and process one or multiple PDF documents.
* Extract and preprocess document text automatically.
* Split documents into semantic chunks for efficient retrieval.
* Generate embeddings using Sentence Transformers.
* Store embeddings in a FAISS vector database.
* Retrieve the most relevant document chunks based on user queries.
* Generate accurate, context-aware answers using an LLM.
* Interactive web interface built with Streamlit.

---

## 🏗️ Architecture

```text
User Uploads PDFs
        │
        ▼
Text Extraction
        │
        ▼
Text Chunking
        │
        ▼
Embedding Generation
        │
        ▼
FAISS Vector Store
        │
        ▼
User Question
        │
        ▼
Similarity Search
        │
        ▼
Relevant Context
        │
        ▼
LLM (Groq / Llama)
        │
        ▼
Final Answer
```

---

## 🛠️ Tech Stack

* Python
* Streamlit
* LangChain
* FAISS
* Sentence Transformers
* Groq API
* Llama Models
* PyPDF2 (or PDF processing library)
* Git

---

## 📂 Project Structure

```text
DocuMind-AI/
│
├── app.py
├── requirements.txt
├── utils/
│   ├── pdf_loader.py
│   ├── text_splitter.py
│   ├── embeddings.py
│   └── vector_store.py
├── data/
├── assets/
└── README.md
```

---

## ⚙️ Installation

### 1. Clone the repository

```bash
git clone https://github.com/your-username/documind-ai.git
cd documind-ai
```

### 2. Create a virtual environment

```bash
python -m venv venv
```

Activate it:

**Windows**

```bash
venv\Scripts\activate
```

**Linux/macOS**

```bash
source venv/bin/activate
```

### 3. Install dependencies

```bash
pip install -r requirements.txt
```

### 4. Configure environment variables

Create a `.env` file and add your API key:

```env
GROQ_API_KEY=your_api_key_here
```

### 5. Run the application

```bash
streamlit run app.py
```

---

## 💡 How It Works

1. Upload one or more PDF documents.
2. Extract text from each document.
3. Split text into manageable chunks.
4. Generate embeddings for every chunk.
5. Store embeddings in a FAISS vector database.
6. Convert the user's question into an embedding.
7. Retrieve the most relevant document chunks.
8. Send the retrieved context and question to the LLM.
9. Display the generated answer in the Streamlit interface.

---

## 🎯 Use Cases

* Research paper analysis
* Technical documentation search
* Policy and compliance documents
* Educational material
* Business reports
* Knowledge base search

---

## 🔮 Future Improvements

* Conversation memory for follow-up questions.
* Source citation highlighting.
* Support for additional document formats (Word, PowerPoint, TXT).
* User authentication.
* Cloud deployment with Docker and Kubernetes.
* Hybrid retrieval (BM25 + Vector Search).
* Streaming responses.

---

## 📚 Key Concepts

* Retrieval-Augmented Generation (RAG)
* Semantic Search
* Vector Embeddings
* FAISS Vector Database
* Prompt Engineering
* Context-Aware Question Answering

---

## 🤝 Contributing

Contributions, bug reports, and feature suggestions are welcome. Feel free to fork the repository, create a feature branch, and submit a pull request.

